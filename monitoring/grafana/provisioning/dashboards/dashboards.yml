apiVersion: 1

providers:
  - name: 'AZALS ERP Dashboards'
    orgId: 1
    folder: 'AZALS'
    folderUid: 'azals'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
